# For local development, run the manifest worker with `wrangler dev`
# (see worker/README.md) and point the frontend at it here. Leave unset
# (or blank) in production, where the frontend calls a relative /api/prints
# path served from the same origin.
VITE_API_BASE=http://localhost:8787
