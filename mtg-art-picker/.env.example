# For local development, run the Pages Function locally with
# `wrangler pages dev` (see DEPLOY.md) and point the frontend at it here.
# Leave unset (or blank) in production, where the frontend calls a relative
# /api/prints path served from the same origin as the deployed site.
VITE_API_BASE=http://localhost:8788
